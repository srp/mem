import glob
import mem

def build():
    env = mem.nodes.Env(ASCIIDOC_FLAGS = ["--unsafe",
                                          "-a stylesdir=$PWD/stylesheets"])

    mem.tasks.asciidoc.asciidoc(glob.glob("*.txt"), env=env)
